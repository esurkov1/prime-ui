import { Badge } from "prime-ui-kit";

/** Smallest valid usage: default `size` `m`, `variant` `light`, `color` `gray`. */
export default function BadgeCanonicalExample() {
  return <Badge.Root>New</Badge.Root>;
}
